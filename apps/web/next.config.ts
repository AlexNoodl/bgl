import type { NextConfig } from "next";

// Bare skeleton (INFRA-001). Image domains for IGDB-hosted covers/screenshots,
// redirects, and any other real configuration land as the relevant tickets
// (GAME-*, WEB-*) are implemented — see docs/architecture.md.
const nextConfig: NextConfig = {};

export default nextConfig;
