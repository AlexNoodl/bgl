# Copy to `.env` in this same directory and fill in real values.
# `docker compose` in this directory loads `.env` automatically.
# Never commit the real `.env` (already covered by the repo's root .gitignore).

POSTGRES_USER=bgl
POSTGRES_PASSWORD=change-me
POSTGRES_DB=bgl
POSTGRES_PORT=5432
